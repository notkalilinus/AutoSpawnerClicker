pluginManagement {
    repositories {
        maven { url 'https://maven.fabricmc.net/' }  // Fabric repo for plugins
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "AutoSpawnerClicker"
